let age = 30;

age = 20;

console.log(age);

//Reassigning or mutating a variable

const name = "Sky";

console.log(name);

//const variable cannot be reassigned/immutable
//const job;
//Const has to be initialised

//Good practice is to always use const unless you're sure that a varible has to change
//Changing variables has the potential to create bugs

var sky = 12;
console.log(sky);

//Var is legacy (before ES6), works the same way as let. !NEVER USE VAR! 

michael = 200;

console.log(michael);

//Declaration is not locally declared, it becomes globally declared